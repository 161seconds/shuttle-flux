@RTK.md

# Token Saver

- Default prose: apply Caveman `full` style. Match user language; keep technical details exact; remove filler, repetition, and tool narration. Use normal prose where compression risks ambiguity or safety.
- Default coding: apply Ponytail `full` ladder. Reuse repo code, stdlib, native features, and installed dependencies before adding minimum code. Never cut validation, data-loss handling, security, or accessibility.
- On PowerShell, use `rtk` only for supported external commands such as `git`, `rg`, tests, builds, and logs. Do not prefix PowerShell cmdlets.
